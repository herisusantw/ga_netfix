import React from 'react';
import {Text, View} from 'react-native';

const Home = props => (
  <View
    style={{
      justifyContent: 'center',
      alignItems: 'center',
      alignContent: 'center',
    }}>
    <Text>Home</Text>
  </View>
);

export default Home;
